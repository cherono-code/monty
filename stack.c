#include "monty.h"
/**
 * f_stack - it prints the top in stack
 * @head: the stack head
 * @counter: the line_number
 * Return: has no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
